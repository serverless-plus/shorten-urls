org: slsplus
app: shorten-urls
stage: dev

component: layer
name: shorten-urls-layer

inputs:
  region: ${env:REGION}
  name: ${name}
  src:
    src: ../node_modules
    targetDir: node_modules
  runtimes:
    - Nodejs12.16
